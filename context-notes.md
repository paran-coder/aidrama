# AI Drama Challenge v1.2.3 — Context Notes

## 목적
v1.2.2의 기능/DB 구조는 유지하면서 UI 디자인 시스템을 재점검하고 데스크톱 중심의 시각적 정합성과 가독성을 개선한다.

## 이번 버전 확정 범위
- 활성/비활성/성공/경고/오류 상태 컬러 시스템 통일
- 준비기간 제출 버튼의 비활성 상태를 연한 세이지/베이지 톤으로 명확히 구분
- 활성 대표 액션은 OWL 진한 녹색으로 통일
- 상태 배지/알림/경고 박스의 톤 일관성 개선
- 사용자에게 보이는 긴 안내문 문장 단위 줄바꿈 정리
- 랜딩/대시보드/전체현황/마이페이지/관리자 화면의 데스크톱 여백, 카드, 텍스트 폭 점검
- Hero + 5단계 stage + 5단계 thumb 부엉이를 투명 배경 자산으로 교체
- 기존 기능, 인증, 제출 규칙, Supabase 스키마는 변경하지 않음

## 디자인 원칙
1. 구조/계층을 스타일보다 우선한다.
2. 주요 액션은 한 화면에서 하나의 강한 색만 사용한다.
3. 비활성 상태는 흐리게만 하지 않고 배경/글자/테두리를 함께 변경해 상태를 명확히 한다.
4. 알림 색은 저채도 배경 + 명확한 텍스트 대비를 사용한다.
5. 문장 줄바꿈은 브라우저 우연에 맡기지 않고 의미 단위로 분리한다.
6. 투명 캐릭터 자산은 카드의 grid-paper 배경과 직접 어우러지도록 사용한다.
7. 모션은 최소화하고 prefers-reduced-motion을 유지한다.

## 제외
- DB migration 추가
- 성장 규칙 변경
- 제출 판정 변경
- 모바일 전면 리디자인

## 구현 결과
- UI semantic color tokens 적용 완료
- 상단 submit navigation과 실제 submit action의 시각적 위계 분리 완료
- 한국어 의미 단위 줄바꿈 규칙 적용 완료
- 투명 owl WebP 11종 적용 및 경량화 완료
- dashboard owl panel의 stretch/blank-space 문제 수정 완료
- DB 변경 없음

## 검증 메모
순수 규칙/구조 테스트와 TS/TSX 구문 변환 검사는 통과했다. 현재 실행환경에서는 npm dependency 설치가 타임아웃되어 dependency-aware typecheck/build는 Vercel에서 확인해야 한다.

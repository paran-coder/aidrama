# v1.2.3 Migration Guide

## DB 변경 여부
**추가 Supabase SQL migration은 없습니다.**

기존 운영 DB에 아래 migration이 이미 적용되어 있다면 그대로 사용합니다.

```text
001_init.sql
002_v1_1_2_ops.sql
003_v1_2_0_growth.sql
004_v1_2_1_hotfix.sql
```

`001`~`004`를 다시 실행하지 않습니다.

## 배포 순서
1. 현재 운영 코드를 백업합니다.
2. GitHub에 v1.2.3 코드를 반영합니다.
3. `.env.local`은 GitHub에 올리지 않습니다.
4. push 후 Vercel Production 배포를 확인합니다.
5. 일반 사용자로 로그인해 랜딩/대시보드/제출/전체현황/마이페이지를 확인합니다.
6. 관리자 로그인 후 운영 관리/접근 관리/초대코드 화면을 확인합니다.

## v1.2.3 확인 포인트
- 부엉이 이미지에 불투명 사각 배경이 보이지 않음
- 준비기간 제출 CTA는 연한 세이지 비활성 상태
- 실제 실행 가능한 주요 CTA는 진한 OWL 녹색
- 첫 제출 안내에서 `가능합니다.` 다음 줄에 `이번 주는 준비 기간입니다.`가 표시됨
- 상단 `이번 주 제출`은 내비게이션 강조색으로 표시되어 본문 CTA와 구분됨

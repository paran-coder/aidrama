import { AppShell } from "@/components/app-shell";
import { updatePasswordAction, updateProfileAction } from "@/lib/actions/profile";
import { getAppContext } from "@/lib/page-context";

export default async function MyPage({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}) {
  const {user,profile,isAdmin}=await getAppContext(); const q=await searchParams; const error=typeof q.error==="string"?q.error:""; const saved=q.saved==="1"; const password=q.password==="1";
  return <AppShell displayName={profile.display_name} isAdmin={isAdmin}><div className="mx-auto max-w-2xl"><p className="eyebrow">Account</p><h1 className="mt-3 text-4xl font-black tracking-[-.05em]">마이페이지</h1>{error&&<div role="alert" className="mt-6 rounded-2xl border border-red-200 bg-red-50 p-4 text-sm font-bold text-[var(--danger)]">{error}</div>}{(saved||password)&&<div role="status" className="mt-6 rounded-2xl border border-emerald-200 bg-emerald-50 p-4 text-sm font-bold text-[var(--success)]">{saved?"프로필이 저장되었습니다.":"비밀번호가 변경되었습니다."}</div>}<section className="card mt-8 rounded-[2rem] p-6"><h2 className="text-xl font-black">프로필</h2><form action={updateProfileAction} className="mt-5 space-y-4"><label className="block text-sm font-bold">표시 이름<input className="input-field mt-2" name="displayName" defaultValue={profile.display_name} maxLength={40} required/></label><label className="block text-sm font-bold">이메일<input className="input-field mt-2 opacity-70" value={user.email??""} disabled readOnly/></label><button className="primary-button">정보 저장</button></form></section><section className="card mt-4 rounded-[2rem] p-6"><h2 className="text-xl font-black">비밀번호 변경</h2><form action={updatePasswordAction} className="mt-5 space-y-4"><label className="block text-sm font-bold">새 비밀번호<input className="input-field mt-2" name="password" type="password" minLength={8} required/></label><button className="secondary-button">비밀번호 변경</button></form></section></div></AppShell>;
}
